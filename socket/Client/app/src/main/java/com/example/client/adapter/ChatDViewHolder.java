package com.example.client.adapter;

import android.view.View;

public class ChatDViewHolder extends BaseViewHolder {

    public ChatDViewHolder(View view) {
        super(view);
    }

    @Override
    public void setData(Object object) {
        super.setData(object);
    }
}